services:
  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_DB: esonge
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data

  app:
    image: leeanghoo2/pine-mushroom-shop:main
    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  db:
